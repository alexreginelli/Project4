<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Baronrails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body>
    
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary px-4">
      <div class="container-fluid">
      <%= link_to "Bar on Rails", dashboard_path, class: "nav-link text-light me-3" %>
        <% if current_user %>
          <div class="navbar-nav">
            <%= link_to "Search cocktails", cocktails_path, class: "nav-link text-light me-3" %>
            <%= link_to "Search ingredient", ingredients_path, class: "nav-link text-light me-3" %>

            <% if current_user.role.downcase == "manager" %>
              <%= link_to "Create cocktail", new_cocktail_path, class: "nav-link text-light me-3" %>
              <%= link_to "Add ingredient", new_ingredient_path, class: "nav-link text-light me-3" %>
            <% end %>

            <% if current_user.role.downcase == "admin" %>
              <%= link_to "View users", users_path, class: "nav-link text-light me-3" %>
            <% end %>

            <%= link_to "Logout", logout_path, method: :delete, class: "nav-link text-light" %>
          </div>
        <% end %>
      </div>
    </nav>

    <%= yield %>
  </body>
</html>
